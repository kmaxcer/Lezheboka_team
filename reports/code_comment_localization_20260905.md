# Локализация комментариев кода — 2026-09-05

Проверены рабочие Python-файлы `app.py`, `src/`, `scripts/` и `tests/`.
Англоязычные поясняющие комментарии переведены на русский; имена полей,
CLI-флаги, URL, машинные идентификаторы и служебные директивы (`noqa`) не
изменялись, потому что они являются частью интерфейса или синтаксиса инструментов.

Проверки:

- поиск английских строк, начинающихся с `#`, в рабочих каталогах — пустой результат;
- `python -m compileall -q app.py src scripts tests` — успешно;
- `pytest -q` — 15 passed.

Поведение моделей, входные данные и submission-файлы не менялись.

После финальной чистки повторная проверка # комментариев и compileall прошли успешно.
