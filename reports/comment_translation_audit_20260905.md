# Аудит комментариев и документации кода

Дата: 2026-09-05.

Переведены на русский язык комментарии и docstring в рабочих файлах `app.py`, `src/`, `scripts/` и `tests/`. Логика, имена функций, формулы, CLI-аргументы и служебные директивы линтеров (`noqa`, `type: ignore`) не изменялись.

Проверка поиском английских пояснительных комментариев и docstring выполнена по всем Python-файлам проекта. Остались только технические директивы линтеров и строки CSS/кода.

Проверки:

- `python -m py_compile app.py src/*.py scripts/*.py tests/*.py` — успешно;
- `python -m pytest -q` — 15 passed.
